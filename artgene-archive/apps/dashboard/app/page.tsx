export default function Home() {
  return (
    <main style={{ fontFamily: "monospace", padding: "2rem" }}>
      <h1>Tinsel — Biosecurity Screening Dashboard</h1>
      <p>Submit a sequence to run it through the gate pipeline.</p>
      {/* Phase 2: wire up ScreenForm + GateResultTable components */}
    </main>
  );
}
